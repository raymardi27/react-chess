# Local Docker Postgres ( matches docker-compose.dev.yml )
# PGHOST=localhost
# PGPORT=5432
# PGUSER=chess_user
# PGPASSWORD=chesspw
# PGDATABASE=chessdb

# Single DB URL ( uncomment if using )
DATABASE_URL=postgres://chess_user:chesspw@localhost:5432/chessdb